pre-commit:
  commands:
    format:
      glob: "*.{c,h,cpp,hpp}"
      run: mise exec -- clang-format -i {staged_files}
      stage_fixed: true # automatically add the formatted files back to the commit
